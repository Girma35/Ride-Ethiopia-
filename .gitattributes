# Set the default behavior to automatically normalize line endings.
* text=auto

# Explicitly set files that should always have CRLF line endings on checkout.
*.cmd text eol=crlf
*.bat text eol=crlf

# Explicitly set files that should always have LF line endings on checkout.
*.sh text eol=lf
